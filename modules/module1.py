# modules are files in the same directory that contain functions, or dictionaries sometimes both.
#  that can be imported  in another file(module)
# there are inbuilt modules like random, string, platform , etc
# random is a function to randomly select specific items,
# string is a function that contains ascii characters like , lower, uppercase letters, digits, and 
# punctuations(including special chars)
# platform is a fuction that determines the system os type
# modules can be made 

import platform

user_system = platform.system()

print(user_system)